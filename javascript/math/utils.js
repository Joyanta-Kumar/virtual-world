export function getClosestPoint(point, points, searchArea = Number.MAX_SAFE_INTEGER) {
  let minDist = Number.MAX_SAFE_INTEGER;
  let closestPoint = null;

  return closestPoint;
}

export function getDistance(start, end) {
  return Math.round(Math.hypot(start.x - end.x, start.y, end.y));
}